{
  "name": "cloudsaver-backend",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda API for CloudSaver cost optimization findings",
  "type": "module",
  "main": "index.mjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check index.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1140.0"
  }
}
